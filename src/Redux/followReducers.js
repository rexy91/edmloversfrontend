const initialState={}

const followReducer = (state = initialState, action) => {

    // switch(action.type){

    //     case 'example':
    //         return {}
        
    //     default: return {} 
    // }

}

export default followReducer
